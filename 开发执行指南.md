# 鲜达生鲜小程序 · 多人并行开发执行指南

> **本文目标：** 让一位新手技术负责人，能按步骤指挥 3-5 人团队完成项目开发。  
> **阅读对象：** 团队负责人（需会基础 Git 命令）。  
> **开发团队：** 1 位架构师 + 1 位后端 + 2-3 位前端。

---

## 第一步：环境准备（所有人）

### 1.1 安装必要软件

| 软件 | 版本要求 | 下载地址 |
|------|----------|----------|
| Node.js | 18.x LTS | https://nodejs.org/ |
| pnpm | 8.x | `npm install -g pnpm` |
| Git | 最新版 | https://git-scm.com/ |
| GitHub CLI (gh) | 最新版 | `winget install GitHub.cli` 或 https://cli.github.com/ |
| 微信开发者工具 | 稳定版 | https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html |
| Docker Desktop | 最新版 | https://www.docker.com/products/docker-desktop/ |
| VS Code | 最新版 | https://code.visualstudio.com/ |

**验证安装：**
```bash
node -v          # 应输出 v18.x.x
pnpm -v          # 应输出 8.x.x
git --version    # 应输出 2.x.x
gh --version     # 应输出 gh version x.x.x
docker --version # 应输出 24.x.x
```

### 1.2 配置 Git 身份信息

每个人在自己的电脑上执行：

```bash
# 配置 Git 身份信息
git config --global user.name "你的姓名"
git config --global user.email "你的邮箱"

# 让 Git 自动使用 GitHub CLI 认证（ push 时无需输入密码）
git config --global credential.helper "!gh auth git-credential"
```

> **注意：** 执行本指南前，请确保已运行 `gh auth login` 完成 GitHub 登录。若尚未登录，参考附录 **Q6**。

---

## 第二步：创建代码仓库（负责人操作）

### 2.1 初始化 Git 仓库

在 `c:\Users\1\Desktop\FreshDash` 目录下执行：

```bash
# 进入项目目录
cd c:\Users\1\Desktop\FreshDash

# 初始化 Git 仓库
git init

# 添加所有文件到暂存区
git add .

# 提交第一个版本
git commit -m "init: 项目初始化，包含任务拆分文档和 PRD"
```

### 2.2 创建远程仓库并推送（使用 GitHub CLI）

```bash
# 创建公开的远程仓库，并自动推送本地代码
gh repo create freshdash --public --source=. --push
```

如果仓库已存在（或需要私有仓库）：
```bash
# 创建私有仓库
gh repo create freshdash --private --source=. --push
```

**检查点：** 执行 `gh repo view --web` 自动打开浏览器，能看到 `tasks/` 目录和 PRD 文件，说明成功。

---

## 第三步：搭建项目骨架（架构师，1-2 天）

### 3.1 创建开发分支

```bash
# 确保在 main 分支
git checkout main

# 拉取最新代码（如果有）
git pull origin main

# 创建项目骨架分支
git checkout -b feat/project-setup
```

### 3.2 按 Task 00 搭建工程

详细步骤见 `tasks/00-project-setup/TASK.md`，核心动作：

1. **初始化 Monorepo**：根目录配置 `pnpm-workspace.yaml`
2. **创建共享类型包**：`packages/shared-types/`
3. **初始化四个应用**：
   - `apps/miniapp` —— Taro + React + TS（用户端）
   - `apps/supplier` —— Taro + React + TS（商家端）
   - `apps/admin` —— Vite + React + TS + Ant Design Pro（管理后台）
   - `apps/server` —— Fastify + Prisma + PostgreSQL（后端）
4. **配置 Docker**：`docker-compose.yml` 包含 PostgreSQL + Redis
5. **编写接口契约文档**：`apps/server/docs/API_INTERFACE.md`

### 3.3 提交并合并

```bash
# 添加所有修改
git add .

# 提交
git commit -m "feat: 项目骨架搭建完成，包含 Monorepo、Docker、共享类型、接口契约"

# 推送分支
git push origin feat/project-setup
```

然后发起 Pull Request 并合并：

```bash
# 创建 PR
gh pr create --base main --title "feat: 项目骨架搭建完成" --body "包含 Monorepo、Docker、共享类型、接口契约"

# 合并到 main（--auto 表示 CI 通过后自动合并，若无需 CI 可直接合并）
gh pr merge --merge --delete-branch
```

**检查点：** 合并后，其他开发者执行 `git pull origin main` 能拿到完整项目结构。

---

## 第四步：分发任务给团队成员

项目骨架合并到 `main` 后，通知所有开发者：

### 4.1 每个人拉取代码

```bash
# 克隆仓库（如果是第一次）
gh repo clone 18701745572/freshdash

# 进入目录
cd freshdash

# 安装所有依赖
pnpm install
```

### 4.2 每个人创建自己的开发分支

| 负责人 | 任务 | 执行的命令 |
|--------|------|-----------|
| 后端开发 | 后端 API | `git checkout -b feat/backend-api` |
| 前端 A | 用户端小程序 | `git checkout -b feat/user-miniapp` |
| 前端 B | 商家端小程序 | `git checkout -b feat/supplier-miniapp` |
| 前端 C | 管理后台 | `git checkout -b feat/admin-dashboard` |

**关键规则：**
- 每个人**只能从 `main` 分支切出新分支**，不能从别人的分支切
- 分支名必须和表格中一致，方便识别

---

## 第五步：并行开发（所有人，2-3 周）

### 5.1 后端开发流程

后端开发参考 `tasks/01-backend-api/TASK.md`，按 Phase 分批交付：

```bash
# 开发过程中，每天下班前提交代码
git add .
git commit -m "feat: 完成订单创建和支付回调接口"
git push origin feat/backend-api
```

**每完成一个 Phase，通知前端可以替换 Mock。**

### 5.2 前端开发流程（Mock 先行）

前端三人参考各自 `TASK.md`，**先用 Mock 数据开发页面**：

```bash
# 示例：用户端开发
cd apps/miniapp
pnpm dev:weapp
```

在 `apps/miniapp/src/services/mock/` 下写死数据，先把页面跑起来。

**等后端通知某模块接口就绪后：**
1. 修改 API 层的 `BASE_URL` 或开关
2. 把 Mock 数据注释掉，切到真实接口
3. 测试数据是否正常

### 5.3 每天同步（重要！）

**早会 10 分钟，每人回答 3 个问题：**
1. 昨天完成了什么？
2. 今天计划做什么？
3. 有没有被阻塞？（需要谁配合）

---

## 第六步：同步最新主分支（每天或每次联调前）

`main` 分支会不断更新（比如架构师修了个配置，后端交了新接口），每个人需要定期同步：

```bash
# 1. 保存当前修改（如果有未提交的）
git stash

# 2. 切换到 main
git checkout main

# 3. 拉取最新代码
git pull origin main

# 4. 切回自己的分支
git checkout feat/你的分支名

# 5. 把 main 的最新代码合并到自己的分支
git merge main

# 6. 恢复刚才保存的修改（如果有）
git stash pop
```

**如果合并时出现冲突**（Conflict）：
- 打开冲突文件，找到 `<<<<<<<` `=======` `>>>>>>>` 标记
- 保留正确的代码，删除标记
- 重新提交：
```bash
git add .
git commit -m "merge: 同步 main 分支最新代码"
```

---

## 第七步：联调（第 3 周）

### 7.1 启动后端服务

```bash
cd apps/server

# 启动数据库和 Redis
docker-compose up -d

# 执行数据库迁移
npx prisma migrate dev

# 启动后端开发服务器
pnpm dev
```

### 7.2 启动前端项目

**用户端小程序：**
```bash
cd apps/miniapp
pnpm dev:weapp
```
用微信开发者工具打开 `apps/miniapp/dist` 目录。

**管理后台：**
```bash
cd apps/admin
pnpm dev
```
浏览器打开 `http://localhost:5173`。

### 7.3 联调检查清单

| 模块 | 检查项 | 通过 |
|------|--------|------|
| 登录 | 微信登录成功，返回 JWT | ☐ |
| 商品 | 首页商品列表正常显示 | ☐ |
| 购物车 | 加购、修改数量、删除正常 | ☐ |
| 订单 | 创建订单 → 支付 → 回调 → 状态更新 | ☐ |
| 派单 | 后台派单后商家端可见 | ☐ |
| 发货 | 商家点击发货，用户端状态变"待收货" | ☐ |
| 推广员 | 绑定关系正确，佣金计算无误 | ☐ |
| 提现 | 小额自动打款，大额人工审核 | ☐ |

---

## 第八步：合并代码到主分支（Code Review）

### 8.1 发起 Pull Request（使用 GitHub CLI）

每个人开发完成后：

```bash
# 确保代码已提交并推送
git add .
git commit -m "feat: 用户端小程序开发完成"
git push origin feat/user-miniapp

# 直接通过命令行创建 Pull Request，自动打开浏览器填写详情
gh pr create --base main --title "feat: 用户端小程序开发完成" --body "详见 tasks/02-user-miniapp/TASK.md 验收标准"
```

`gh pr create` 执行后会提示选择：
- 按 **Enter** 使用默认标题，或手动输入
- 按 **e** 在编辑器中填写详细描述（可选）
- 按 **Enter** 提交，或按 **w** 在浏览器中打开再提交

**指定 Reviewer（审查人）：**
```bash
gh pr create --reviewer "同事用户名"
```

### 8.2 Review 检查项

Reviewer 检查以下项目：
- [ ] 代码能正常运行，无语法错误
- [ ] 没有提交 `node_modules`、`.env` 等敏感文件
- [ ] 没有 `console.log` 调试代码残留
- [ ] 符合项目代码规范（ESLint 通过）
- [ ] 与 `main` 分支无冲突

### 8.3 合并（使用 GitHub CLI）

Review 通过后，由负责人或 Reviewer 执行：

```bash
# 查看当前待合并的 PR 列表
gh pr list

# 合并指定 PR（--merge 表示普通合并，--squash 表示压缩提交）
gh pr merge 1 --merge --delete-branch
```

参数说明：
- `--merge`：保留完整提交历史（推荐）
- `--squash`：把所有提交压缩成一条（适合功能开发分支）
- `--delete-branch`：合并后自动删除远程分支

**注意：** 开发者自己不能合并自己的代码，必须由其他人 Review 后操作。

---

## 第九步：测试与上线

### 9.1 本地测试

```bash
# 启动完整环境
docker-compose up -d
cd apps/server && pnpm dev
cd apps/admin && pnpm dev
```

### 9.2 微信小程序上传

```bash
cd apps/miniapp
pnpm build:weapp
```
用微信开发者工具点击"上传"，填写版本号和备注。

### 9.3 后端部署

```bash
cd apps/server
pnpm build
docker build -t freshdash-server .
```

---

## 附录：常见问题速查

### Q1: `pnpm install` 报错？
```bash
# 先清除缓存
pnpm store prune
rm -rf node_modules
pnpm install
```

### Q2: Git 推代码提示 "failed to push"？
```bash
# 先拉取远程最新代码，合并后再推送
git pull origin main --rebase
git push origin feat/你的分支名
```

### Q3: 微信开发者工具打不开 dist 目录？
- 确认 `pnpm dev:weapp` 编译成功
- 确认项目 appid 已配置（`project.config.json` 中的 `appid`）
- 若还没有小程序账号，先用测试号：`touristappid`

### Q4: Docker 启动失败？
```bash
# 检查 Docker Desktop 是否正在运行
docker --version

# 重启 Docker 服务
docker-compose down
docker-compose up -d
```

### Q5: 忘记同步 main，导致大量冲突？
```bash
# 方案：放弃本地修改，重新拉取（会丢失未提交的修改，谨慎使用）
git fetch origin
git reset --hard origin/main
```

### Q6: 如何登录 GitHub CLI？
```bash
# 执行登录命令
gh auth login
```

按提示选择：
- `What account do you want to log into?` → **GitHub.com**
- `What is your preferred protocol?` → **HTTPS**
- `Authenticate Git with your GitHub credentials?` → **Yes**
- `How would you like to authenticate?` → **Login with a web browser**

终端会显示一次性代码，按 **Enter** 打开浏览器粘贴授权即可。

**验证登录：**
```bash
gh auth status
```
应显示 `✓ Logged in to github.com as 你的用户名`。

---

## 任务时间线总览

| 天数 | 任务 | 负责人 |
|------|------|--------|
| Day 1 | 环境准备 + 创建仓库 | 负责人 |
| Day 1-2 | 项目骨架搭建（Task 00） | 架构师 |
| Day 3 | 所有人拉代码、切分支、开干 | 全员 |
| Day 3-14 | 四线并行开发 | 全员 |
| Day 15-18 | 联调 + 修 Bug | 全员 |
| Day 19-20 | 测试验收 + 部署上线 | 负责人 |
